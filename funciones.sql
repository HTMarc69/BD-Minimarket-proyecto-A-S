--
-- funciones de control de flujo
--

-- case
/*
select 
	case case case_value
    when when_value then senteces
    [when when_value then senteces]
    [else senteces]
    end,
from
where
*/

select * from orders;
